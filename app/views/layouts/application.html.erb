<!DOCTYPE html>
<html lang="en">
  <head>
    <title>bluebird</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="assets/css/bootstrap-responsive.css" rel="stylesheet">

    <!-- Latest compiled and minified JavaScript -->
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css">
   
    <!-- Optional theme -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap-theme.min.css"> 

    <!-- Google jQuery CDN -->
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
    <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/jquery-ui.min.js"></script> 

    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %> 
  </head>

  <body class="<%= params[:controller].parameterize %>-controller" data-twttr-rendered="true" data-spy="scroll" data-target="#ss-sidebar">  

    <header>
      <%= render :partial => 'layouts/header' %> 
    </header>

    <% if params[:controller] == 'root' %>
      <video preload="auto" autoplay="autoplay" loop="true" class="video">
        <source src="http://static.baboom.com/splash/vid/b_bang_splash.mp4" type="video/mp4">
      </video>
      <div class="container-fluid">
        <div class = "row-fluid">
         <%= yield %>  
        </div>
      </div>
    <% else %> 
      <div class="container bs-docs-container" id="ss-sidebar">
        <div class = "row"> 
          <%= yield %>
        </div>
      </div>
    <% end %>

    <footer> 
      <%= render :partial => 'layouts/footer' %> 
    </footer>
  </body>
</html>
